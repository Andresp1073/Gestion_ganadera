@using Logic
@using Model
@inject NavigationManager NavManager

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h4 class="fw-semibold">Lista de Compradores</h4>
        <a href="/buyer/create" class="btn btn-success">+ Crear Comprador</a>
    </div>

    <div class="table-responsive shadow-sm border rounded bg-white">
        <table class="table table-striped table-bordered align-middle mb-0">
            <thead class="table-light">
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Cédula</th>
                    <th>Número de Teléfono</th>
                    <th style="width: 100px;">Opciones</th>
                </tr>
            </thead>
            <tbody>
                @if (buyers != null && buyers.Any()) @* Añadido null check por seguridad *@
                {
                    @foreach (var buyer in buyers)
                    {
                        <tr>
                            <td>@buyer.IdBuyer</td>
                            <td>@buyer.Name</td>
                            <td>@buyer.Cc</td>
                            <td>@buyer.PhoneNumber</td>
                            <td>
                                <a href="@($"/buyer/update/{buyer.IdBuyer}")" class="btn btn-sm btn-primary w-100">Editar</a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="text-center">No hay compradores disponibles.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@code {
    private List<BuyersEntity> buyers = new();

    protected override async Task OnInitializedAsync()
    {
        try
        {
            var buyersLog = new BuyersLog();
            buyers = await Task.Run(() => buyersLog.BuyersList());

            // Ordenar al cargar
            buyers = buyers.OrderBy(b => b.IdBuyer).ToList();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error al cargar compradores: {ex.Message}");
            buyers = new List<BuyersEntity>(); // Prevenir errores por listas nulas
        }
    }
}
